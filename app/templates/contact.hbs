{{page-title (if this.locale.isKorean "문의하기 | Cleantech" "Contact | Cleantech")}}

<section class="space-y-16">
  <header class="max-w-3xl space-y-4">
    <p class="text-sm font-semibold uppercase tracking-[0.35em] text-blue-600">{{this.copy.eyebrow}}</p>
    <h1 class="text-4xl font-bold leading-tight text-slate-900 sm:text-5xl">{{this.copy.title}}</h1>
    <p class="text-lg leading-relaxed text-slate-600">{{this.copy.description}}</p>
  </header>

  <div class="grid gap-10 lg:grid-cols-[minmax(0,1.1fr)_minmax(0,0.9fr)]">
    <div class="space-y-8">
      <section class="rounded-3xl border border-slate-200 bg-gradient-to-br from-white via-white to-blue-50/40 p-8 shadow-sm">
        <h2 class="text-xl font-semibold text-slate-900">{{this.copy.bulletTitle}}</h2>
        <ul class="mt-6 space-y-3 text-slate-600">
          {{#each this.copy.bullets as |item|}}
            <li class="flex items-start gap-3">
              <span class="mt-1 h-2.5 w-2.5 flex-none rounded-full bg-blue-500"></span>
              <span class="leading-relaxed">{{item}}</span>
            </li>
          {{/each}}
        </ul>
      </section>

      <section class="rounded-3xl border border-slate-200 bg-white p-8 shadow-sm">
        <div class="flex items-center justify-between gap-4">
          <h2 class="text-xl font-semibold text-slate-900">{{this.copy.supportTitle}}</h2>
          <span class="rounded-full bg-blue-50 px-3 py-1 text-xs font-semibold text-blue-600">{{this.copy.responseTime}}</span>
        </div>

        <div class="mt-6 grid gap-4 sm:grid-cols-2">
          <div class="rounded-2xl border border-slate-100 bg-slate-50/80 p-4">
            <div class="text-sm font-medium text-slate-500">Email</div>
            <a
              href="mailto:ct4138605@gmail.com"
              class="mt-1 block text-lg font-semibold text-slate-900 transition hover:text-blue-600"
            >ct4138605@gmail.com</a>
          </div>
          <div class="rounded-2xl border border-slate-100 bg-slate-50/80 p-4">
            <div class="text-sm font-medium text-slate-500">{{if this.locale.isKorean '전화' 'Phone'}}</div>
            <a
              href="tel:024202844"
              class="mt-1 block text-lg font-semibold text-slate-900 transition hover:text-blue-600"
            >02-420-2844~5</a>
          </div>
        </div>

        <div class="mt-6 flex items-center gap-3 rounded-2xl bg-blue-50/70 px-4 py-3 text-sm text-blue-700">
          <svg aria-hidden="true" viewBox="0 0 24 24" class="h-5 w-5 fill-blue-600/80">
            <path d="M12 2a9.93 9.93 0 0 0-9.89 8.85 1 1 0 0 0 .18.71 1 1 0 0 0 .63.39L5 12.24V13a7 7 0 0 0 14 0v-.76l2.08-.29a1 1 0 0 0 .63-.39 1 1 0 0 0 .18-.71A9.93 9.93 0 0 0 12 2Zm0 18a5 5 0 0 1-5-5v-.34l1.23-.18a3 3 0 0 0 2.51-3V8a1 1 0 0 1 2 0v3.48a3 3 0 0 0 2.51 3L17 14v1a5 5 0 0 1-5 5Z" />
          </svg>
          <div>
            <div class="font-semibold">Cleantech Customer Care</div>
            <div class="text-slate-600">MON–FRI 09:00–18:00 (KST)</div>
          </div>
        </div>
      </section>

      <section class="rounded-3xl border border-slate-200 bg-slate-900 p-6 text-slate-100 shadow-sm">
        <h2 class="text-lg font-semibold">Project Snapshot</h2>
        <p class="mt-2 text-sm text-slate-300">
          {{if this.locale.isKorean
            '현장 도면, 사진, 요구 성능 자료가 있다면 함께 전달해 주세요. 보다 빠르게 솔루션을 제안해 드릴 수 있습니다.'
            'Have layout drawings, photos, or specs ready? Attach them in your reply so we can respond with a precise proposal.'}}
        </p>
      </section>
    </div>

    <section class="rounded-3xl border border-slate-200 bg-white p-8 shadow-xl shadow-blue-500/10">
      <form class="space-y-6" {{on "submit" this.submitForm}} aria-live="polite">
        <div class="grid gap-6 sm:grid-cols-2">
          <label class="flex flex-col gap-2 text-sm font-medium text-slate-700">
            <span>{{this.copy.nameLabel}}</span>
            <input
              class="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 text-base font-medium text-slate-900 shadow-sm transition focus:border-blue-500 focus:bg-white focus:outline-none focus:ring-2 focus:ring-blue-200"
              type="text"
              value={{this.name}}
              required
              {{on "input" (fn this.updateField "name")}}
            />
          </label>

          <label class="flex flex-col gap-2 text-sm font-medium text-slate-700">
            <span>{{this.copy.emailLabel}}</span>
            <input
              class="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 text-base font-medium text-slate-900 shadow-sm transition focus:border-blue-500 focus:bg-white focus:outline-none focus:ring-2 focus:ring-blue-200"
              type="email"
              value={{this.email}}
              required
              {{on "input" (fn this.updateField "email")}}
            />
          </label>

          <label class="flex flex-col gap-2 text-sm font-medium text-slate-700">
            <span>{{this.copy.phoneLabel}}</span>
            <input
              class="w-full rounded-xl border border-dashed border-slate-200 bg-white/60 px-4 py-3 text-base text-slate-900 transition focus:border-blue-500 focus:bg-white focus:outline-none focus:ring-2 focus:ring-blue-200"
              type="text"
              value={{this.phone}}
              placeholder="010-1234-5678"
              {{on "input" (fn this.updateField "phone")}}
            />
          </label>

          <label class="flex flex-col gap-2 text-sm font-medium text-slate-700">
            <span>{{this.copy.companyLabel}}</span>
            <input
              class="w-full rounded-xl border border-dashed border-slate-200 bg-white/60 px-4 py-3 text-base text-slate-900 transition focus:border-blue-500 focus:bg-white focus:outline-none focus:ring-2 focus:ring-blue-200"
              type="text"
              value={{this.company}}
              {{on "input" (fn this.updateField "company")}}
            />
          </label>
        </div>

        <label class="flex flex-col gap-2 text-sm font-medium text-slate-700">
          <span>{{this.copy.messageLabel}}</span>
          <textarea
            class="min-h-[160px] w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 text-base text-slate-900 shadow-sm transition focus:border-blue-500 focus:bg-white focus:outline-none focus:ring-2 focus:ring-blue-200"
            value={{this.message}}
            required
            placeholder={{this.copy.messagePlaceholder}}
            {{on "input" (fn this.updateField "message")}}
          ></textarea>
        </label>

        <div class="space-y-3">
          {{#if this.isSuccess}}
            <div class="rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-700">
              {{this.copy.successMessage}}
            </div>
          {{/if}}

          {{#if this.isError}}
            <div class="rounded-xl border border-rose-200 bg-rose-50 px-4 py-3 text-sm text-rose-700">
              {{this.errorMessage}}
            </div>
          {{/if}}

          <button
            type="submit"
            class="flex w-full items-center justify-center gap-2 rounded-xl bg-gradient-to-r from-blue-600 via-blue-500 to-indigo-500 px-6 py-3.5 text-base font-semibold text-white shadow-lg shadow-blue-500/30 transition hover:from-blue-500 hover:via-blue-500 hover:to-indigo-500 disabled:cursor-not-allowed disabled:opacity-60"
            disabled={{this.submitDisabled}}
          >
            {{#if this.isSubmitting}}
              <svg aria-hidden="true" viewBox="0 0 24 24" class="h-5 w-5 animate-spin fill-white/80">
                <path d="M12 2a10 10 0 0 0-9.54 7.06 1 1 0 1 0 1.92.56A8 8 0 1 1 4 12a1 1 0 0 0-2 0 10 10 0 1 0 10-10Z" />
              </svg>
            {{/if}}
            <span>{{this.buttonLabel}}</span>
          </button>

          <p class="text-xs text-slate-500">{{this.copy.privacyNote}}</p>
        </div>
      </form>
    </section>
  </div>
</section>
